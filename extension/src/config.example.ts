// 复制为 config.ts（gitignored）后填入自己 relay 的实际值
// 编译插件前必须存在 config.ts

export const RELAY_WS_URL = 'wss://your-relay-domain.com/ws'
export const BRIDGE_ACCESS_KEY = 'replace-with-your-bridge-access-key'
